<!DOCTYPE html>
<html>
<head>
  <title>Chest</title>
  <%= stylesheet_link_tag "header", media: "all" %>
  <%= stylesheet_link_tag "action", media: "all" %>
  <%= javascript_include_tag "https://kit.fontawesome.com/7c1ce35e86.js", crossorigin: "anonymous" %>
</head>

<body>
<div class="action-content-wrapper">
  <h1>Chest</h1>
  <div class="action-action-controls">
    <div class="action-filter-dropdown">
      <button class="action-filter-btn">
        Filter <i class="fa-solid fa-sliders"></i>
      </button>
      <ul class="action-filter-options">
        <li><%= link_to "All",   action_path %></li>
        <li><%= link_to "Chest",   chest_path %></li>
        <li><%= link_to "Back",    back_path  %></li>
        <li><%= link_to "Arm",     arm_path   %></li>
        <li><%= link_to "Leg",     leg_path   %></li>
        <li><%= link_to "Shoulder", shoulder_path %></li>
      </ul>
    </div>

    <input class="action-search-bar" type="text" placeholder="Search" />
  </div>

  <div class="action-actions-grid">
    <div class="action-actions-grid">
      <% if @actions.present? %>
        <% @actions.each do |action| %>
          <%= render(ActionCardComponent.new(action: action)) %>
        <% end %>
      <% else %>
        <p style="text-align:center; margin-top: 20px;">No actions found for Chest.</p>
      <% end %>
    </div>
  </div>


</div>
</body>
</html>
